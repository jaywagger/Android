<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exercise">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".recycler.RecyclerAmong" />
        <activity android:name=".temp.temp1" />
        <activity android:name=".intent.ClickFunction" />
        <activity android:name=".carview.cardView" />
        <activity android:name=".MainActivity" />
        <activity android:name=".childView.bottomAppBar" />

        <activity android:name=".Among.bottom.bottomNavigationBar"/>
        <activity android:name=".childView.ChildFirstPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>